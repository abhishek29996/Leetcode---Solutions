class Solution:
    def reverseString(self, s: List[str]) -> None:

        # Two Pointers
        left = 0
        right = len(s) - 1

        while left < right:
            s[left], s[right] = s[right], s[left]

            left += 1
            right -= 1

        return s

        # n = len(s)  # cannot use this because we have to do it in same item
        # res = []
        # for j in range(n):
        #     char = s.pop()
        #     res.append(char)
        # return res
