class Solution:
    def isAdjacentDiffAtMostTwo(self, s: str) -> bool:
        for j in range(len(s) - 1):
            diff = abs(int(s[j]) - int(s[j+1]))
            if diff > 2:
                return False
        return True
